Package package {
    name='ceylon.math.decimal';
    doc="Decimal floating point arithmetic. The focus of this package is the
    `Decimal` type which performs computations using decimal
    floating point arithmetic. `Rounding` is used to specify how results of
    calculations should be rounded.";
    shared=true;
}